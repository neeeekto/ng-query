import { InjectionToken } from '@angular/core';

export const NG_QUERY_DEFAULT_CONFIG = new InjectionToken(
  'NG_QUERY_DEFAULT_CONFIG'
);

export const NG_GC_DEFAULT_CACHE_TIMER = new InjectionToken(
  'NG_GC_DEFAULT_CACHE_TIMER'
);
